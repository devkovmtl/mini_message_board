<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <div>
      <% if (messages.length > 0) { %> <% for (let i = 0; i < messages.length;
      i++) {%>
      <div>
        <p><%= messages[i].text %> from <%= messages[i].user%></p>
        <p>at: <%= messages[i].added.toLocaleDateString() %></p>
      </div>
      <% } %> <% } else { %>
      <p>No messages yet</p>
      <% } %>
    </div>
  </body>
</html>
